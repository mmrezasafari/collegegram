components:
  schemas:
    MentionedAndSavedPage:
      type: object
      properties:
        id:
          type: string
          format: uuid
        caption:
          type: string
        onlyCloseFriends:
          type: boolean
        createdAt:
          type: string
          format: date-time
        images:
          type: array
          items:
            $ref: "#/components/schemas/PostImage"