components:
  schemas:
    Post:
      type: object
      properties:
        id:
          type: string
        caption:
          type: string
        user:
          $ref: "#/components/schemas/User"
        images:
          type: array
          items:
            type: object
            properties:
              id: 
                type: string
              url: 
                type: string
      